package com.example.MyCollection.utils;import android.content.ContentValues;import android.content.Context;import android.database.DatabaseErrorHandler;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteOpenHelper;import android.util.Log;/** * Created by lilian on 10/10/14. */public class SQLiteHelper extends SQLiteOpenHelper {    //getting the name of the class    private static final String TAG = DatabaseErrorHandler.class.getSimpleName();    //static variables    public static final int DATABASE_VERSION = 1;    public static final String DATABASE_NAME = "validation table";    public static final String TABLE__NAME = "validation";    public static final String TABLE__FULL_NAME = "validation";    public static final String TABLE__PASSWORD = "validation";    Constants myconstants;//public SQLiteHelper (Context context){    //super(context,DATABASE_NAME,null,DATABASE_VERSION);//}    public SQLiteHelper(Context context, String name, SQLiteDatabase.CursorFactory factory, int version) {        super(context, DATABASE_NAME, null, DATABASE_VERSION);    }    public void onCreate(SQLiteDatabase db) {        String CREATE_TABLE_VALIDATION = "create table" + TABLE__NAME + "(" + Constants.KEY_ID + "integer key ," + Constants.KEY_USERNAME + "text," +                Constants.KEY_Email + "text," + Constants.KEY_fullName + "text ," + Constants.KEY_PASSWORD + "text);";        db.execSQL(CREATE_TABLE_VALIDATION);    }    @Override    public void onUpgrade(SQLiteDatabase sqLiteDatabase, int oldVersion, int newVersion) {        Log.w(TAG, "Warning!!! you are upgrading the db version" + newVersion);    }    //deleting data from sqlite.    public void deleteAll() {        //SQLiteDatabase db = this.getWritableDatabase();        // db.delete(TABLE_NAME,null,null);        //db.execSQL("delete * from"+ TABLE_NAME);        db.execSQL("TRUNCATE table" + TABLE_NAME);        db.close();    }    //update sqlite database.    public int updateStatus(String email, String fullName, String password) {        ContentValues cv = new ContentValues();        String where = fullName + "=" + "ekanta" + " and " + password + "=" + "your password" + "=" + email + "your email";        return updateStatus(email, fullName, password);    }}